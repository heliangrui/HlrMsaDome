server:
  port: 8082
spring:
  application:
    name: openFeignProvider
  cloud:
    nacos:
      discovery:
        server-addr: 127.0.0.1:8848
        loadbalancer:
          cache:
            enabled: false
        enabled: true

dubbo:
  enabled: false
    