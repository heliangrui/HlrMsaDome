server:
  port: 8088
spring:
  application:
    name: testapimsa
  cloud:
    nacos:
      config:
        enabled: false
      discovery:
        enabled: false
        watch:
          enabled: false
dubbo:
  application:
    register-mode: instance
    metadata-type: remote
  registry:
    address: nacos://127.0.0.1:8848
  protocol:
    name: dubbo
    threads: 400
    port: -1
  consumer:
    check: false
    retries: 0
    filter: traceFilter
  provider:
    retries: 0
    timeout: 3000
    filter: traceFilter
    
