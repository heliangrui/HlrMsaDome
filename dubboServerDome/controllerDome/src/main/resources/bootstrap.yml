server:
  port: 8088
spring:
  application:
    name: testwebmsa1112222222
    
dubbo:
  application:
    register-mode: instance
  registry:
    address: nacos://172.16.10.87:8848
  protocol:
    name: dubbo
    accesslog: false
    threads: 400
  consumer:
    check: false
    retries: 0
    filter: traceFilter
  provider:
    retries: 0
    timeout: 3000
    filter: traceFilter
    
