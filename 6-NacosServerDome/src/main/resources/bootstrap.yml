server:
  port: 80
spring:
  application:
    name: labelapimsa
  cloud:
    nacos:
      config:
        server-addr: 192.168.16.200:8848
        file-extension: yaml
        namespace: dev
        group: ${spring.application.name}
dubbo:
  enabled: false

management:
  endpoints:
    web:
      exposure:
        include: "*"